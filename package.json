{
  "name": "root",
  "private": true,
  "scripts": {
    "setup": "npx lerna bootstrap",
    "test": "npx lerna run test",
    "build": "npx lerna run build"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
